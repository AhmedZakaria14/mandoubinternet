const slides=[...document.querySelectorAll('.slide')];const dots=document.querySelector('.dots');let current=0,timer;slides.forEach((_,i)=>{const b=document.createElement('button');b.setAttribute('aria-label',`عرض الصورة ${i+1}`);b.addEventListener('click',()=>show(i));dots.appendChild(b)});const dotButtons=[...dots.children];function show(i){slides[current].classList.remove('active');dotButtons[current].classList.remove('active');current=(i+slides.length)%slides.length;slides[current].classList.add('active');dotButtons[current].classList.add('active');clearInterval(timer);timer=setInterval(()=>show(current+1),4500)}document.querySelector('.prev').addEventListener('click',()=>show(current-1));document.querySelector('.next').addEventListener('click',()=>show(current+1));document.querySelector('#year').textContent=new Date().getFullYear();show(0);
